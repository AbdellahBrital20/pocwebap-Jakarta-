# REST API Configuration
api.base.url=http://localhost:8080
api.timeout=30000

# Test Mode (true = use JSON files, false = use real API)
app.test.mode=true

# Session timeout in minutes
session.timeout=30